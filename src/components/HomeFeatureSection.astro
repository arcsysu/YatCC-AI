---
import HomeProductShowcase, {
  type ShowcaseFeature,
} from "@/components/HomeProductShowcase.astro";

interface Props {
  id: string;
  eyebrow: string;
  title: string;
  direction?: "left" | "right";
  features: ShowcaseFeature[];
}

const props = Astro.props;
---

<HomeProductShowcase {...props} />
